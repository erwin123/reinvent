import { FiltermediaPipe } from './filtermedia.pipe';

describe('FiltermediaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltermediaPipe();
    expect(pipe).toBeTruthy();
  });
});
